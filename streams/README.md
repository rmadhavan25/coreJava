# Streams API

## Topics to Practice
- [ ] Creating streams — `stream()`, `Stream.of()`, `Stream.generate()`, `Stream.iterate()`
- [ ] Intermediate ops — `map`, `filter`, `flatMap`, `distinct`, `sorted`, `peek`, `limit`, `skip`
- [ ] Terminal ops — `collect`, `forEach`, `reduce`, `count`, `findFirst`, `anyMatch`, `allMatch`
- [ ] `Collectors` — `toList()`, `toSet()`, `toMap()`, `groupingBy()`, `partitioningBy()`, `joining()`
- [ ] `Optional` — creation, `map`, `flatMap`, `orElse`, `orElseThrow`, `ifPresent`
- [ ] Primitive streams — `IntStream`, `LongStream`, `DoubleStream`
- [ ] `mapToInt`, `mapToObj`, `boxed`
- [ ] Parallel streams — use cases, pitfalls, thread safety
- [ ] Collectors chaining — `groupingBy` with downstream collector
- [ ] Custom `Collector`
- [ ] Infinite streams with `limit`
