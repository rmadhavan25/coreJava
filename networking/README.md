# Networking & HTTP

## Topics to Practice
- [ ] `Socket` and `ServerSocket` — TCP client/server
- [ ] `DatagramSocket` — UDP
- [ ] Java 11 `HttpClient` — synchronous and async requests
- [ ] `HttpRequest`, `HttpResponse`, `BodyHandlers`
- [ ] WebSocket with `HttpClient`
- [ ] `URL` and `URLConnection` (legacy)
- [ ] `InetAddress` — DNS lookup
- [ ] Non-blocking I/O with `SocketChannel`, `Selector`
- [ ] Simple HTTP server with `com.sun.net.httpserver`
