# JDBC & Database Connectivity

## Topics to Practice
- [ ] JDBC architecture — Driver, Connection, Statement, ResultSet
- [ ] `DriverManager` vs `DataSource`
- [ ] `Statement` vs `PreparedStatement` vs `CallableStatement`
- [ ] SQL injection prevention with `PreparedStatement`
- [ ] Transactions — `commit()`, `rollback()`, `setAutoCommit(false)`
- [ ] Savepoints
- [ ] Batch operations — `addBatch()`, `executeBatch()`
- [ ] Connection pooling concepts (HikariCP, c3p0)
- [ ] `ResultSetMetaData`
- [ ] Stored procedure calls
- [ ] JDBC with try-with-resources
- [ ] `RowMapper` pattern
