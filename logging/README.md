# Logging

## Topics to Practice
- [ ] Logging levels — TRACE, DEBUG, INFO, WARN, ERROR
- [ ] SLF4J as the facade — `LoggerFactory.getLogger()`
- [ ] Logback configuration — `logback.xml`, appenders, patterns
- [ ] Log4j2 configuration — `log4j2.xml`
- [ ] Rolling file appenders
- [ ] MDC — `MDC.put()`, `MDC.get()`, `MDC.clear()` for request context
- [ ] Structured/JSON logging
- [ ] Log4Shell awareness (security)
- [ ] Async logging
- [ ] Logging best practices — what to log, what not to log
