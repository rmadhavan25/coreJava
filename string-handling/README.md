# String Handling

## Topics to Practice
- [ ] `String` immutability internals & String pool
- [ ] `String` vs `StringBuilder` vs `StringBuffer`
- [ ] Common `String` methods — `substring`, `split`, `replace`, `trim`, `strip`, `indexOf`
- [ ] `String.format()` and `formatted()` (Java 15+)
- [ ] Text blocks (Java 15+)
- [ ] `String.join()`, `String.valueOf()`
- [ ] `intern()` method and when to use it
- [ ] Regex with `Pattern` and `Matcher`
- [ ] `StringTokenizer` vs `split()`
- [ ] `CharSequence` interface
- [ ] String comparison — `equals()` vs `==` vs `compareTo()`
