# Collections Framework

## Topics to Practice
- [ ] List — ArrayList, LinkedList, CopyOnWriteArrayList
- [ ] Set — HashSet, LinkedHashSet, TreeSet, EnumSet
- [ ] Map — HashMap, LinkedHashMap, TreeMap, EnumMap, IdentityHashMap
- [ ] Queue — PriorityQueue, ArrayDeque, LinkedList as Queue
- [ ] Deque — ArrayDeque, LinkedList as Deque
- [ ] Stack (prefer ArrayDeque)
- [ ] Concurrent collections — ConcurrentHashMap, ConcurrentLinkedQueue, BlockingQueue
- [ ] `Collections` utility class — sort, binarySearch, unmodifiableList, synchronizedList
- [ ] `Arrays` utility class
- [ ] Comparable vs Comparator
- [ ] Fail-fast vs fail-safe iterators
- [ ] `Map.Entry` iteration patterns
- [ ] Java 9+ immutable collections — `List.of()`, `Map.of()`, `Set.of()`
