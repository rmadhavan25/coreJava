# Multithreading & Concurrency

## Topics to Practice
- [ ] Thread lifecycle — NEW, RUNNABLE, BLOCKED, WAITING, TIMED_WAITING, TERMINATED
- [ ] `Runnable` vs `Thread` vs `Callable`
- [ ] `wait()`, `notify()`, `notifyAll()` — producer-consumer pattern
- [ ] `ExecutorService` — `newFixedThreadPool`, `newCachedThreadPool`, `newSingleThreadExecutor`
- [ ] `ScheduledExecutorService`
- [ ] `Future` and `get()` with timeout
- [ ] `CompletableFuture` — `thenApply`, `thenCompose`, `thenCombine`, `allOf`, `anyOf`
- [ ] `ReentrantLock`, `tryLock()`, `lockInterruptibly()`
- [ ] `ReadWriteLock` — `ReentrantReadWriteLock`
- [ ] `StampedLock` (Java 8+)
- [ ] `CountDownLatch` — await for N events
- [ ] `CyclicBarrier` — synchronize N threads at a point
- [ ] `Semaphore` — limit concurrent access
- [ ] `Exchanger`
- [ ] `Phaser` (flexible barrier)
- [ ] `ThreadLocal` and `InheritableThreadLocal`
- [ ] Thread pools — sizing strategy, queue types, rejection policies
- [ ] Virtual threads (Java 21+) — `Thread.ofVirtual()`
- [ ] Structured concurrency (Java 21+)
- [ ] Deadlock, livelock, starvation — detection & prevention
